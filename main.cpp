#include <iostream>

using namespace std;

static bool isOdd(int num) {
    return num % 2 != 0;
}

int main() {
    int rows, cols;

    cout << "Enter the number of rows: ";
    cin >> rows;
    cout << "Enter the number of columns: ";
    cin >> cols;

    return 0;
}
